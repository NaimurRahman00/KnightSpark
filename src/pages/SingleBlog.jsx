import { useLoaderData } from "react-router-dom";

const SingleBlog = () => {
    const blog = useLoaderData();
    return (
        <div>
            
        </div>
    );
};

export default SingleBlog;